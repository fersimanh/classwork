[project]
name = "demo-streamlit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fersimanh", email = "fernandosiman2012@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "plotly>=7.1.0",
    "streamlit>=1.64.0",
    "yfinance>=1.7.0",
]

[project.scripts]
demo-streamlit = "classwork:main"

[tool.uv.build-backend]
module-name = "classwork"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
